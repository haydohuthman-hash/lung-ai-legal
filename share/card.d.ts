import type { ProgressShareSnapshot } from './model.js';
export function renderProgressCard(snapshot: ProgressShareSnapshot, options?: { mascotUrl?: string; displayFont?: string; bodyFont?: string }): Promise<Blob>;
